# This is simply a migration module, as lights.py has been moved, and it
# should not be modified.

"""A migration module for Camera."""

import Render

Camera = Render.Camera
ViewProviderCamera = Render.ViewProviderCamera
